import styled from "styled-components"

export const UnstyledLinkedCard = styled.div`
  && {
    text-decoration: unset;
    color: black;
    font-style: unset;
    cursor: pointer;

    &:hover {
      text-decoration: unset;
      font-style: unset;
    }
  }
`
